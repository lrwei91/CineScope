{
  "name": "cinescope",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs && python3 -m compileall -q scripts/automation && python3 -m unittest discover -s tests -p 'test_*.py'",
    "check:syntax": "node scripts/check-syntax.mjs",
    "check:data": "node scripts/validate-data.mjs",
    "check": "npm run check:syntax && npm test && npm run check:data",
    "build:site": "node scripts/build-site.mjs"
  }
}
